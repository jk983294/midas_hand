package com.victor.midas.calculator.stats;

/**
 * this contains all aggregation calculator needed for all target calculator
 * the ugliness is not all aggregation calculators are needed,
 * for example, DayStatsAggregator are not needed, this one is purely statistic purpose
 * we may want to refactor it to some dedicated case, so that we can use reflection
 */
public class AggregatorWrapper {
}
